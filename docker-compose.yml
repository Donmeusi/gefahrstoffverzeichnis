version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./gefahrstoffe.db:/app/gefahrstoffe.db
      - ./uploads:/app/uploads
    environment:
      - FLASK_APP=main.py
      - FLASK_ENV=production
    restart: unless-stopped
